export default interface Student {
    /**
     * Unique identifier for the student.
     */
    id: string;
    firstName: string;
    lastName: string;
    phoneNumber: string;
    gpa: string;
    street: string;
    city: string;
    state: string;
    zipCode: string;
}